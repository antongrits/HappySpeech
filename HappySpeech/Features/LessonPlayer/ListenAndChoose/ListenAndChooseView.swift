import SwiftUI

// MARK: - ListenAndChooseView

struct ListenAndChooseView: View {

    var body: some View {
        Text("ListenAndChoose")
            .font(TypographyTokens.title())
            .foregroundStyle(ColorTokens.Kid.ink)
    }
}

#Preview {
    ListenAndChooseView()
}
