import Foundation

// MARK: - Settings VIP Models
// Backlog: see backlog.md for implementation tickets

enum SettingsModels {

    // MARK: - Fetch
    enum Fetch {
        struct Request {}
        struct Response {}
        struct ViewModel {}
    }

    // MARK: - Update
    enum Update {
        struct Request {}
        struct Response {}
        struct ViewModel {}
    }
}
